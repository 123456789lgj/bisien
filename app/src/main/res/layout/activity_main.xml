<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
<!-- 微信的底部是#F8F8F8 -->
    <RadioGroup
        android:id="@+id/rgRadioGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:layout_alignParentBottom="true"
        android:background="#F8F8F8"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rbHome"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/selector_home"
            android:drawablePadding="2dp"
            android:gravity="center"
            android:text="首页"
            android:textColor="@color/selector_bottom_text"
            android:textSize="10sp" />

        <RadioButton
            android:id="@+id/rbAlarm"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:button="@null"
            android:drawableTop="@drawable/selector_alarm"
            android:drawablePadding="2dp"
            android:gravity="center"
            android:text="告警"
            android:textColor="@color/selector_bottom_text"
            android:textSize="10sp" />

        <RadioButton
            android:id="@+id/rbStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:button="@null"
            android:gravity="center"
            android:text="状态"
            android:drawableTop="@drawable/selector_status"
            android:drawablePadding="2dp"
            android:textColor="@color/selector_bottom_text"
            android:textSize="10sp" />

        <RadioButton
            android:id="@+id/rbMy"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_weight="1"
            android:button="@null"
            android:gravity="center"
            android:drawableTop="@drawable/selector_my"
            android:drawablePadding="2dp"
            android:text="我的"
            android:textColor="@color/selector_bottom_text"
            android:textSize="10sp" />
    </RadioGroup>

    <FrameLayout
        android:id="@+id/mainFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/rgRadioGroup">

    </FrameLayout>


</RelativeLayout>